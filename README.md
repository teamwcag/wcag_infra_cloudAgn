# Remediation Platform Infra (Networking P1)
Cloud-agnostic networking via Terraform. Start with envs/dev, then clone to uat/prod.

Quick start:
  cd infra/envs/dev
  terraform init
  terraform plan
  terraform apply
